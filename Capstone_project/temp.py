key = 'Seattle, WA'
for (k,v) in populations.items():
    print(populations[key])


I have a cursor that is iterating through rows of flight data... in here are max counts of trips.
I need to on each iteration of a row of data...

record the max count of trips (already managed this)
record the corresponding max population for the location associated to that city
